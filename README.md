# JS-2026

## git Commands

git status – Check the status of files (modified, staged, untracked).
git add <file> – Stage a specific file for commit.
git add . – Stage all changed files in the current directory.
git add -A or git add --all – Stage all changes (new, modified, deleted).
git commit -m "message" – Commit staged changes with a message.
git commit -am "message" – Stage and commit all tracked files in one step.
git commit --amend – Modify the most recent commit (e.g., fix message).
